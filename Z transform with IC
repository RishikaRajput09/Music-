N=10;
n= 0:1:N-1;
x=[ones(1,N)]; 
subplot(4,1,1),stem(n,x);
 
a=[1 0.8];
b=[1];
 
y1=[1];
h=filtic(b,a,y1)
subplot(4,1,2),stem(h);
 
k=filter(b,a,x,h)
subplot(4,1,3),stem(n,k);
 
y2=[(5/9)-((16/45)*(-0.8).^n)];
subplot(4,1,4),stem(n,y2);

 
